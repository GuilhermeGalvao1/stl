#include <bits/stdc++.h>

using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define f first 
#define s second 

typedef long long ll;

int main(){ _
    vector<int> v;

    v.push_back(2);
    v.push_back(3);

    cout << v.front() << " " << v.back() << endl;
    cout << v[1] << endl;
}